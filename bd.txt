----Prueba VentureSoft

CREATE TABLE HU_CAT_MONEDA(
NUM_CIA NUMBER(4,0)NOT NULL,
CLAVE_MONEDA VARCHAR2(3) NOT NULL, 
DESRIPCION VARCHAR2(30),
SIMBOLO VARCHAR2(5),
ABREVIACION VARCHAR2(5),
MONEDA_CORRIENTE VARCHAR(1),
STATUS VARCHAR2(1),
CONSTRAINT PK_HU_CAT_MONEDA PRIMARY KEY(NUM_CIA, CLAVE_MONEDA)
);


INSERT INTO HU_CAT_MONEDA VALUES (1234, 'PMX', 'PESOS MEXICANOS', '$', 'MXN', 'S', 'A');
COMMIT;
SELECT * FROM HU_CAT_MONEDA;